using System.Text.RegularExpressions;

namespace Library_management.Service;
public class ValidationService
{
    public void processRequest(string request)
    {
        // Regex.IsMatch(request, ^[\d])
    }
}